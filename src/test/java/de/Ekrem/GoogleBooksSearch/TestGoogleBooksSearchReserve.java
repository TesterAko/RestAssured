package de.Ekrem.GoogleBooksSearch;

/*
import org.junit.Test;

import static io.restassured.RestAssured.given;
import static org.hamcrest.CoreMatchers.equalTo;


public class TestGoogleBooksSearchReserve {


    @Test
    public void testGoogleBooksSearchForTestAutomation() {
        given()
                .queryParam("q", "Testautomatisierung")
                //+Beginn Rest Assured Anfrage, hier werden Parameter definiert
                .when()
                //+wenn
                .get("https://www.googleapis.com/books/v1/volumes")
                //+HTTP Anfrage an URL sende
                .then()
                //+dann
                .assertThat()
                //+vergleiche
                .statusCode(200)
                .body("kind", equalTo("books#volumes"));
                //++vergleiche ob books#volumes text im kind parameter im body
                // im Qeryparam der Website googleapis ... volumes gegeben ist
                //und ob Statuscode 200 hat !!

    }
}
*/